
## 10. 表：question_feedback（题目反馈）

用途：存储学生对题目的疑问和反馈。

```sql
CREATE TABLE IF NOT EXISTS question_feedback (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
  student_id BIGINT NOT NULL COMMENT '学生ID',
  question_id BIGINT NOT NULL COMMENT '题目ID',
  exam_id BIGINT COMMENT '考试ID',
  content TEXT NOT NULL COMMENT '反馈内容',
  reply_content TEXT COMMENT '回复内容',
  status INT DEFAULT 0 COMMENT '状态 0:未处理 1:已处理',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  reply_time DATETIME COMMENT '回复时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目反馈表';
```

接口关联说明：
- `question_feedback.question_id` 关联 `question.id`
- 反馈查询接口可按该字段回填关联题目
- 学生端仅展示安全字段
- 教师端可展示完整题目
- 反馈管理页可基于该关联直接编辑 `question` 表中的题目内容

## 12. 变更补充

### wrong_book

新增字段：
```sql
ALTER TABLE wrong_book
ADD COLUMN note TEXT NULL COMMENT '学生错题笔记' AFTER practice_correct_count;
```

说明：
- `note` 用于学生在错题练习时记录个人笔记
- 笔记跟随错题本记录保存，可反复修改

## 11. 表：question_review（题目讲评）

用途：存储教师发布的题目讲评内容。

```sql
CREATE TABLE IF NOT EXISTS question_review (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
  question_id BIGINT NOT NULL COMMENT '题目ID',
  teacher_id BIGINT NOT NULL COMMENT '教师ID',
  title VARCHAR(255) COMMENT '讲评标题',
  content TEXT NOT NULL COMMENT '讲评内容',
  publish_status INT DEFAULT 0 COMMENT '发布状态 0:草稿 1:已发布',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目讲评表';
```

接口关联说明：
- `question_review.question_id` 关联 `question.id`
- 题目讲评查询接口可按该字段回填关联题目

设计补充说明：
- 反馈管理中的“编辑题目”不新增表
- 修改内容仍落在 `question` 表，资料分析题干仍落在 `stem` 表

## 13. 表：review（考试讲评）

用途：存储某张试卷的考试讲评。

设计说明：
- `review.exam_id` 关联 `exam.id`
- `review.content` 存储结构化讲评内容
- 内容可包含一段总评 `summary`
- 也可包含多条题目讲评 `questionReviews`
- `questionReviews[].questionId` 必须属于该试卷
## 14. 学生讲评查看补充说明

- 本次仅补充学生端讲评查看入口与接口权限校验
- 数据库表结构无新增、无字段变更
- 学生讲评可见范围仍基于 `review.exam_id`、`exam_class.exam_id`、`class_student.class_id` 三者关联判断
- 学生只能看到自己所在班级对应试卷、且状态为已发布的考试讲评
- `questionReviews` 响应项可回填 `questionContentImageUrl` 与 `questionOptions`
- 这两个字段来自 `question` 表中的题干配图与选项数据，不新增存储
## 15. 教师试题搜索补充说明

- 本次仅补充教师端试题管理的检索能力
- 数据库表结构无新增、无字段变更
- 教师端 `question` 列表查询支持按 `content`、`analysis`、`category` 模糊匹配
- 当搜索词为纯数字时，同时支持按 `question.id` 精确匹配

## 16. Wrong Book Category Filter

- No schema change in this update.
- The wrong book filter now reuses question.category.
- wrong_book.question_id joins question.id, then filters by question category.
- The UI 'All Types' dropdown now represents question categories instead of answer types.

## 17. 通知全体发送补充说明

- 本次无新增表
- 本次无字段变更
- `notification.user_id` 仍保存单个接收人
- 教师未选班级时，系统按全部学生展开写入通知
- 全体通知仍是一人一条记录，便于已读状态独立保存

